#  고가용성 웹 서버 구축 미니 프로젝트 - DB 파트

이 저장소는 프로젝트의 데이터베이스 인프라(MariaDB) 및 관리 도구(Adminer)의 Docker 실행 환경을 담고 있습니다.

## 포함된 서비스
1. **MariaDB**: 서비스의 데이터를 저장하는 메인 데이터베이스
2. **Adminer**: 웹 브라우저에서 DB를 관리할 수 있는 GUI 도구

## 실행 방법
터미널에서 해당 폴더로 이동 후 아래 명령어를 입력하세요.
```bash
docker compose up -d
